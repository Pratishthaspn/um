Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
python-dotenv==1.0.1

# Needed once DATABASE_URL points to a hosted Postgres DB for deployment
# (SQLite's local file won't persist on Vercel's serverless filesystem)
psycopg2-binary==2.9.9
